dependencies:
  override:
    - pip install -r requirements.txt
    - pip install -r dev_requirements.txt
    - pip install mysql-python
test:
  override:
    - cd extras && python project/manage.py test classifieds --settings=settings_circleci
